{
    "build": {
      "env": {
        "PYTHON_VERSION": "3.11"
      },
      "buildCommand": "pip install -r api/requirements.txt",
      "startCommand": "uvicorn api.data_app:app --host 0.0.0.0 --port $PORT"
    }
  }
  