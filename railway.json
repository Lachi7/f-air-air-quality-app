{
    "build": {
      "env": {
        "PYTHON_VERSION": "3.11"
      },
      "rootDirectory": "api",
      "buildCommand": "pip install -r requirements.txt",
      "startCommand": "uvicorn data_app:app --host 0.0.0.0 --port $PORT"
    }
  }
  